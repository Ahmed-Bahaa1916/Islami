import 'package:flutter/material.dart';

class SabhaaTap extends StatelessWidget {
  const SabhaaTap({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        color: Colors.blue,
      ),
    );
  }
}
